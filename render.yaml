services:
  - type: web
    name: jamcraft-backend
    runtime: node
    buildCommand: npm install && pip3 install yt-dlp --break-system-packages && yt-dlp --update-to nightly
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18